1.  Melhorar estrutura da tabela Hash. Não utilizar várias posições para referenciar o mesmo Documento. Algo do tipo => hash_table["THE"]  :=> [1, [49,2,67,89,100], [7, 3,5,8,16]]
[qtd, [doc,pos1,pos2,pos3], [doc2,pos1,pos2,pos3]]

2.  Implementar entrada de dados para busca simples -> E e OU

3. Indice recebe como entrada => <term; p; pos; docId>, 
  * term => termo encontrado em um documento
  * p => parte do documento onde o termo ocorre ( T => titulo do documento ou C => corpo do documento)
  * pos =>  posição onde o termo ocorre 
  * docId => identificadorunico do documento onde term ocorre

4. Processamento de ordenação pelo metodo dos cossenos.

5. Processamento por frases. (busca por frases)
